replicaCount: 1

image:
  repository: apache/apisix
  pullPolicy: IfNotPresent
  tag: "3.9.1-debian"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: "traefik"
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

config:
  orionHost: "scorpio"
  orionPort: 9090
  mintakaHost: "mintaka"
  mintakaPort: 8080
  keycloakClientId: "apisix-pep"
  keycloakClientSecret: "978ad148-d99b-406d-83fc-578597290a79"
  keycloakDiscoveryUrl: "http://keycloak-keycloakx-http:80/realms/fiware-server/.well-known/openid-configuration"