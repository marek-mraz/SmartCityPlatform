apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexurbis-manager.fullname" . }}
  labels:
    {{- include "nexurbis-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexurbis-manager.selectorLabels" . | nindent 4 }}