apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "scorpioBroker-aaio.fullname" . }}
  labels:
    {{- include "scorpioBroker-aaio.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "scorpioBroker-aaio.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "scorpioBroker-aaio.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "scorpioBroker-aaio.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: DBHOST
              value: {{ if .Values.postgresql.enabled }}"{{ include "scorpioBroker-aaio.fullname" . }}-postgres"{{ else }}"{{ .Values.postgresql.host }}"{{ end }}
            - name: DBPORT
              value: "{{ .Values.postgresql.port }}"
            - name: DBUSER
              value: "{{ .Values.postgresql.user }}"
            - name: DBPASS
              {{- if .Values.postgresql.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.existingSecret }}
                  key: {{ .Values.postgresql.existingSecretPasswordKey | default "password" }}
              {{- else }}
              value: "{{ .Values.postgresql.password }}"
              {{- end }}
            - name: DBNAME
              value: "{{ .Values.postgresql.database }}"
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: "jdbc:postgresql://{{ if .Values.postgresql.enabled }}{{ include "scorpioBroker-aaio.fullname" . }}-postgres{{ else }}{{ .Values.postgresql.host }}{{ end }}:{{ .Values.postgresql.port }}/{{ .Values.postgresql.database }}?ApplicationName=aio-runner"
            - name: QUARKUS_DATASOURCE_REACTIVE_URL
              value: "postgresql://{{ if .Values.postgresql.enabled }}{{ include "scorpioBroker-aaio.fullname" . }}-postgres{{ else }}{{ .Values.postgresql.host }}{{ end }}:{{ .Values.postgresql.port }}/{{ .Values.postgresql.database }}?ApplicationName=aio-runner"
          ports:
            - containerPort: 9090
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
