{{- if .Values.applications }}
{{- $secrets := .Values.secretsEnabled }}
{{- $release := .Values.release }}
{{- $source := .Values.source }}
{{- $team := .Values.team }}
{{- $host := .Values.host }}
{{- $tlsSecret := .Values.tlsSecret }}
{{- $argoNamespace := .Values.argoNamespace -}}
{{- range $app := .Values.applications }}
{{- if $app.source }}
{{- $source = $app.source }}
{{- end }}
{{- if $app.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
  - resources-finalizer.argocd.argoproj.io
{{- if or (eq $release "ci-cd") (eq $release "cluster-ops") (eq $release "pm") }}
  name: {{ .name }}
{{- else }}
  name: {{ $.Values.destination_namespace | default $release }}-{{ .name }}
{{- end }}
{{- if $argoNamespace }}
  namespace: {{ $argoNamespace }}
{{- end }}
  labels:
    destination-namespace: {{ $.Values.destination_namespace | default .destination }}
{{- if .values }}
{{- if .values.fullnameOverride }}
{{- if contains .values.fullnameOverride .name }}
    app.kubernetes.io/instance: {{ .values.fullnameOverride }}
{{- end }}
{{- else }}
    app.kubernetes.io/instance: {{ .name }}
{{- end }}
{{- end }}
spec:
  destination:
    namespace: {{ $.Values.destination_namespace | default .destination | default (printf "%s-%s" $team $release) }}
    server: {{ .cluster_api | default "https://kubernetes.default.svc" }}
  project: {{ .project | default "default" }}
  source:
{{- if or .helm_values .values }}
    helm:
{{- if .helm_values }}
      valueFiles:
{{- toYaml .helm_values | nindent 8 }}
{{- if $app.secretsReady }}
{{- if $secrets }}
        - values-secured.yaml
{{- else }}
        - values-unsecured.yaml
{{- end }}
{{- end }}
{{- end }}
{{- if .values }}
      values: |
{{- tpl (toYaml .values) $ | nindent 8 }}
{{- end }}
{{- if or .releaseName .name }}
      releaseName: {{ .releaseName | default .name }}
{{- end }}
{{- end }}
    {{- if .source_path }}
    path: {{ .source_path | default "." }}
    {{- end }}
    repoURL: {{ $source }}
    targetRevision: {{ .source_ref | default "main" | quote }}
    {{- if not .source_path }}
    chart: {{ .chart_name | default .name }}
    {{- end }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=true
      - CreateNamespace=true
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
        - /spec/template/spec/containers/0/image
    - group: integreatly.org
      kind: GrafanaDataSource
      jsonPointers:
        - /spec/datasources/0/secureJsonData/httpHeaderValue1
{{- if eq .name "platform-secrets" }}
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
        - /stringData
{{- end }}
{{- end }}
{{- end }}
{{- end }}
