apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot-agent-json.fullname" . }}
  labels:
    {{- include "iot-agent-json.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iot-agent-json.selectorLabels" . | nindent 4 }}